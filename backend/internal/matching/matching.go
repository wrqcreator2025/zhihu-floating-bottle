// Package matching 负责候选筛选、语义排序与投递计划。
// 输入：确认目标、发送者活动画像、候选人已确认且开放的经历与画像。
// 必须经历由本人确认的 Experience 满足；活动信息只补充语义排序。
// 排除本人和历史接收者；同瓶有限投递 3–5 人，允许并行接住。
// AI 计算在事务外；插入邀请前锁瓶子行复查状态与剩余名额。
// 没有画像仍可按问题和确认经历匹配；不伪造真人回信。
package matching
