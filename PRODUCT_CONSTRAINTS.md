# 《漂流瓶》长期产品约束

记录日期：2026-09-07。来源：用户明确要求长期记忆的四项硬性要求，以及对后续尝试性质的说明。

## 使用规则

后续整理 PRD、提出方案、设计 HTML 或实现产品前，应先阅读本文。以下硬性要求持续有效，只有用户明确修改硬性要求时才更新；不要用一次视觉尝试或局部方案隐式覆盖它们。

后续用户提供的方案、风格、布局和 HTML 尝试要求，默认属于可修改的探索，不自动升级为长期硬性要求。区分「长期约束」与「本次实验选择」。

## 四项硬性要求

1. **先形成完整 PRD。** 将用户在对话中提供的《漂流瓶》PRD v0.1（共 31 节）整理为正式仓库文档，保留完整功能、产品原则、边界和验证指标。本文不是该 PRD，也不代表整理工作已经完成。若未来无法取得原文，应找回原文，不凭简略记忆重建并声称完整。
2. **完整实现文档功能。** 最终交付必须实现 PRD 中全部规划功能，不能以静态页面或局部核心流程代替完整产品。P0、P1、P2 可用于安排实施顺序，不作为永久省略规划功能的理由；原文明确不做的功能仍不实现。阶段性 HTML 仅为设计验证，不代表功能已完成。
3. **迅速上手，易于理解。** 用户应一眼理解产品用途，并能很快发出自己的内容，不需要花很长时间学习。采用日常、具体的文案和操作，不把设计过程中的抽象概念直接呈现给用户，例如「经验路由」「硬匹配」「目标画像」。减少理解和首次表达的负担。
4. **网页端与手机端。** 提供电脑网页端和手机端体验，分别考虑布局、导航、输入和操作流程，不能仅把电脑版缩小。用户尚未要求手机端必须采用原生 App，不能擅自增加此技术限制。

## 当前状态

## 新增硬性要求：瓶子与长期经历分离

- 求助瓶子可以重复创建。抛出一个之后可写全新的瓶子；旧瓶子独立保留，不要求收回、覆盖旧瓶子才能继续。
- 「我走过的经历」是个人长期资料，确认后持续保留，可编辑、补充和关闭接收；不与某个瓶子绑定，不随瓶子抛出、收回或消失。
- 连续创建不等于已确定多个瓶子同时匹配。与原 PRD 单一主动寻找名额的关系尚未明确，原型不能声称实现了并行真人匹配。

## 新增硬性要求：同一瓶子可多人接住

- 用户明确确认：同一个瓶子允许同时被多个人接住并分别回信，不要求前一人交流结束再流向下一人。
- 每个接收者拥有独立邀请和短连接，回信、一次追问、反馈、未读与封存分别计算，接收者之间不能查看彼此的交流。
- 一人接住不停止其他邀请；一人放行不影响其他人的回信。原 PRD 的有限投递范围继续适用，不扩大为公共求助广场。
- 此要求修改的是单个瓶子的接收人数，不代表允许同一用户同时激活多个瓶子。接口当前保留一个主动寻找名额。

## 新增硬性要求：AI 结合知乎活动进行经历匹配

- AI 根据发送者写下的问题和其可用的知乎活动信息，推测需要哪类经历的人；用户确认或修改目标经历后再抛出瓶子。
- 候选人必须先本人填写或确认经历并开放接收。AI 再结合候选人的经历与其可用的知乎活动信息进行匹配和排序。
- 当前使用 v3 知乎关注流和 v4 知乎搜索构建活动画像。未开放的浏览、点赞、收藏或私密行为不纳入设计。
- 活动信息不能自动证明人生经历，也不能向匹配双方展示对方的原始活动、关注关系或内部画像；知乎接口不可用时退化为问题与已确认经历匹配。

## 新增确定需求：匿名聊天与内容审核

- 原瓶发信者收到回信后可邀请该回信者匿名聊天，对方接受后双方才可发送聊天消息；每段连接独立，允许继续交流，替代旧版“一次追问后自动封存”的限制。
- 从瓶子柜打开相应瓶子，查看原信及已回信者列表，进入对应邀请或会话。拒绝不处罚；支持结束、举报和屏蔽，不开放任意陌生人私信或身份解锁。
- 瓶子投递、回信和匿名聊天消息送达前均须通过 AI 内容审核。骚扰等违规内容不送达；保留原稿，允许修改、重试和申请复核。
- 审核绑定最终内容版本；审核服务失败不能默认通过，活动画像缺失的降级策略不能绕过审核。具体产品流程和验收以 PRD 第 18、21.1、30 节为准。

## 已确认的页面布局（历史设计记录）

- 用户明确确认当前海湾布局（以 `landing-experiment-07.html` 的连续岸线版为准）。后续视觉尝试默认沿用这一布局，不重新改变空间组织。
- 中央大漂流瓶：唯一主视觉，兼顾写新请求、捡起示例来瓶、接住与回信。
- 下方连续海湾岸线：左侧木棚内的瓶子柜保存来往记录，右侧经历小屋编辑长期经历。
- 空间关系由海面、沙滩、光照与落地阴影体现，不恢复显式栈道或流程连接线。抛出向远海，来瓶从远海靠近。
- 蓝色配色为接下来的可修改尝试，尚未确认；重复创建与经历独立仍属于硬性要求。

## 历史状态

- 用户不满意已有 `index.html` 的设计，后续需要重新设计，不将旧设计视为已接受方案。
- 用户已授权一次基于随机字母数字串的独立视觉落地页尝试，产出为 `landing-experiment.html`；该尝试不代表长期风格已定稿。
- 完整产品范围已按用户原稿 31 节整理到 `docs/PRD.md`；真实功能实现仍待开展，不把 HTML 预览计为完整交付。
- 赛事依据为用户提供的「知乎黑客松 2026｜校园新锐季」完整手册，不套用其他场次的规则。PRD 原文关于「AI 场景价值为初审最高权重」的说法未在该手册中获得支持，整理时应标注此差异，不当作已核实的赛事要求。
